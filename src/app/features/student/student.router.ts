import { Routes } from '@angular/router';
import { StudentComponent } from './student.component';

export const STUDENT_ROUTERS: Routes = [
  {
    path: '',
    component: StudentComponent,
  },
];

